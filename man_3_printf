./" _printf Team Project.
./"
.TH man 1 "20 April 2022" "1.0" "_Printf Team Project"
.SH NAME
._Printf
\- This function produces output according to a format.
.SH SYNOPSIS
#include "main.h"

int _printf(const char *format, ...)

_printf[argument]....

.SH DESCRITION
The printf function printd numerical values or ASCII characters of the format specifiers passed to it in the argument.
This _printf include our vrsion of the printf function, the basic and main function.

Return: The number of characters printed (excluding the NULL byte used to end output to string).

Conversion specifiers: There are many different conversion specifiers that can be used for various data types.

%c - print a single character.

%s - print a string.

%d - print an integer as a signed decimal (base 10) number.

%i - print same as "d", integer in base 10.

% - print a percent sign(%).

A conversion specifier begins with a percent sign and ends with the concersion characters shown below.

.SH For example.

_printf("Character: %c", character);

_printf("String: %s", string);

_printf("integer: %d", integer);(or use "%i" for print integer, depends of the case).

>SH SEE ALSO
printf(1), _printf(2), printf(3), sprintf(), vsprintf(), vsnprintf().

.SH BUGS

Haile GG
Olyad T

.SH AUTHOR
